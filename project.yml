name: Starlane
options:
  bundleIdPrefix: com.ravosolutions
  deploymentTarget:
    iOS: "17.0"
  xcodeVersion: "26.2"
  createIntermediateGroups: true
  generateEmptyDirectories: true
  groupSortPosition: top
  defaultConfig: Debug

packages:
  StarlaneCore:
    path: Packages/StarlaneCore

settings:
  base:
    SWIFT_VERSION: "6.0"
    SWIFT_APPROACHABLE_CONCURRENCY: YES
    SWIFT_DEFAULT_ACTOR_ISOLATION: MainActor
    SWIFT_UPCOMING_FEATURE_MEMBER_IMPORT_VISIBILITY: YES
    SWIFT_STRICT_CONCURRENCY: complete
    IPHONEOS_DEPLOYMENT_TARGET: "17.0"
    TARGETED_DEVICE_FAMILY: "1"
    ENABLE_USER_SCRIPT_SANDBOXING: YES
    CODE_SIGN_STYLE: Automatic
    DEVELOPMENT_TEAM: ""
    SWIFT_EMIT_LOC_STRINGS: YES
    LOCALIZATION_PREFERS_STRING_CATALOGS: YES
    ENABLE_PREVIEWS: YES
    STRING_CATALOG_GENERATE_SYMBOLS: NO

targets:
  Starlane:
    type: application
    platform: iOS
    sources:
      - path: Starlane
        excludes:
          - "Info.plist"
    dependencies:
      - package: StarlaneCore
    info:
      path: Starlane/Info.plist
      properties:
        CFBundleDisplayName: Starlane
        CFBundleShortVersionString: $(MARKETING_VERSION)
        CFBundleVersion: $(CURRENT_PROJECT_VERSION)
        UILaunchScreen:
          UIColorName: LaunchBackground
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationPortrait
        UIUserInterfaceStyle: Dark
        UIStatusBarHidden: true
        UIAppFonts:
          - BigShouldersDisplay.ttf
          - InstrumentSans.ttf
        UIViewControllerBasedStatusBarAppearance: false
        UIRequiresFullScreen: true
        ITSAppUsesNonExemptEncryption: false
        LSRequiresIPhoneOS: true
        UIApplicationSupportsIndirectInputEvents: true
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.ravosolutions.starlane
        PRODUCT_NAME: Starlane
        MARKETING_VERSION: 1.0.0
        CURRENT_PROJECT_VERSION: 1
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
        GENERATE_INFOPLIST_FILE: NO
        SUPPORTS_MACCATALYST: NO
        INFOPLIST_KEY_LSApplicationCategoryType: public.app-category.games

  StarlaneTests:
    type: bundle.unit-test
    platform: iOS
    sources:
      - StarlaneTests
    dependencies:
      - target: Starlane
      - package: StarlaneCore
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.ravosolutions.starlane.tests
        GENERATE_INFOPLIST_FILE: YES

schemes:
  Starlane:
    build:
      targets:
        Starlane: all
        StarlaneTests: [test]
    run:
      config: Debug
    test:
      config: Debug
      gatherCoverageData: true
      targets:
        - StarlaneTests
        - package: StarlaneCore/StarlaneCoreTests
    profile:
      config: Release
    analyze:
      config: Debug
    archive:
      config: Release
